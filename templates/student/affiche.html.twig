<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.5.3/dist/css/bootstrap.min.css" integrity="sha384-TX8t27EcRE3e/ihU7zmQxVncDAy5uIKz4rEkgIXeMed4M0jlfIDPvg6uqKI2xXr2" crossorigin="anonymous">
    <title>Document</title>
</head>
<body>

<div class="container pt-2 text-center">
    <h1>affiche student</h1>
   <div class="p-4"> {{ form(form) }}</div>
<table class="table">
    <thead>
    <tr>
        <th scope="col">id</th>
        <th scope="col">nsc</th>
        <th scope="col">email</th>
        <th scope="col">classroom</th>

        <th scope="col">delete or modify</th>
    </tr>
    </thead>
    <tbody>

    <tr {% for student in students %}>
        <th scope="row">{{ student.id }}</th>
        <td>{{ student.NSC }}</td>
        <td>{{ student.Email }}</td>
        <td>{{ student.classroom }}</td>

        <td><a class="btn btn-success" href="{{ path("edit",{"id":student.id}) }}">edit</a>
            <a class="btn btn-danger" href="{{ path("delete",{"id":student.id}) }}">delete</a></td>
    </tr {% endfor %}>

    </tbody>
</table>
</div>
</body>
</html>